# @package _global_
name: ttnet_bicontext
notes: >-
  Baseline model of "Visual Transformation Telling", fusion bicontext into label_ids

defaults:
  - override /model: ttnet_bicontext
  - override /model/generate_cfg: top_k_top_p_no_repeat

criterion:
  loss:
    logit_shift: 0
    label_shift: -1

pl_trainer:
  precision: 32

model:
  decoder_context_fusion: add
